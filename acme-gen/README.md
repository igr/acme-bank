# Acme Bank Generator

⚠️ The models are using primitives on purpose.

```shell
gradlew run
```

The output is in `out/`:

+ `acme.sql`: the SQL file to create the database
+ `input.csv`: the input file for the load generator